export function PaginationBar({ page, pages, onChange, total, label = "Pages", previousLabel = "Previous", nextLabel = "Next" }: { page: number; pages: number; onChange: (page: number) => void; total?: number; label?: string; previousLabel?: string; nextLabel?: string }) {
  if (pages <= 1) return null;
  return <nav aria-label={label} className="flex flex-wrap items-center justify-between gap-3 py-4 text-sm text-secondary"><p>Page {page} of {pages}{total == null ? "" : ` (${total.toLocaleString()} total)`}</p><div className="flex gap-2"><button type="button" disabled={page <= 1} onClick={() => onChange(Math.max(1, page - 1))} className="rounded-lg border border-line-strong px-3 py-2 hover:bg-surface-alt disabled:opacity-50">{previousLabel}</button><button type="button" disabled={page >= pages} onClick={() => onChange(Math.min(pages, page + 1))} className="rounded-lg border border-line-strong px-3 py-2 hover:bg-surface-alt disabled:opacity-50">{nextLabel}</button></div></nav>;
}
