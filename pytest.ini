[pytest]
testpaths = tests
markers =
    unit: marks tests as unit tests (no database fixtures needed)
    integration: marks tests as integration tests
    performance: marks tests as performance benchmarks
    e2e_prod: live verification against a deployed API (skipped unless RBAC_VERIFY_BASE_URL is set)
