Questions for Tracelab:
Data Model & Schemas
What are the Pydantic models for:
Mission (what fields? IDs, objectives, status, timestamps?)
Document (title, content, source_url, created_at?)
Insight (key finding text, confidence score, tags?)
DocumentChunk (chunk text, embeddings, metadata?)
Relationships: How do these entities link?
Does a Mission contain multiple Documents?
Do Documents have multiple Insights?
Are Chunks auto-generated by Tracelab or passed in?
Quality Gates: What validations will fail with 422?
Required fields?
Format constraints (URL validation, date formats)?
Business rules (e.g., "unresolved_contradictions" field must be empty)?
API Design Coordination
Endpoints: Proposed design for agent writes:
POST /api/missions - Creates mission + all nested data in one call?
Or separate: POST /api/missions/{id}/documents, POST /api/missions/{id}/insights?
Authentication: How should DeepSearch authenticate?
API key in header?
Local-only (both services on same machine)?
Error responses: What does a 422 look like?
   {
     "detail": [
       {"loc": ["body", "documents", 0, "source_url"], "msg": "invalid url format"}
     ]
   }
   {     "detail": [       {"loc": ["body", "documents", 0, "source_url"], "msg": "invalid url format"}     ]   }
