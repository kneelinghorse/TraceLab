# Mission B2.3c – Ingestion Integration & Parity

- CLI `scripts/ingest_cli.py` drives document upload and processing flows offline or against deployments.
- Parity reports land in `artifacts/ingestion-parity/` via `scripts/verify_ingestion_parity.py` with metrics + audit trail.
- New pytest integration check exercises CLI → API → pipeline using Markdown corpus.
- Observability kit (`infra/observability/ingestion/`) and runbook document operationalize the workflow.
